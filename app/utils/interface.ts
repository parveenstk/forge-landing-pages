export interface LandingPageData {
    header: {
        desktopText: string
        mobileText: string
    }
    meta: {
        title: string
        description: string
        ogImage: string
    }
}
